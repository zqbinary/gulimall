spring:
  application:
    name: gulimall-auth-server
  cloud:
    nacos:
      discovery:
        server-addr: ss:8848
  thymeleaf:
    cache: false
  redis:
    password: root
  session:
    store-type: redis
server:
  port: 20000